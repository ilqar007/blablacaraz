@model BlaBlaCarAz.BLL.DomainModel.Entities.AppUser

@{
    ViewData["Title"] = Model.NameLastName + "'s public profile";
    // Save today's date.
    var today = DateTime.Today;
    // Calculate the age.
    int? age = null;
    if (Model.Birthdate.HasValue)
    {
        age = today.Year - Model.Birthdate.Value.Year;

        // Go back to the year in which the person was born in case of a leap year
        if (Model.Birthdate.Value.Date > today.AddYears(-age.Value)) age--;
    }
}


<div class="text-center mb-xl mt-xl">
    <div class="account-user-avatar">
        <img width="144" height="144" src="@Url.Action("GetImage","Profile",new { id=Model.Id})" alt="@Model.NameLastName's public profile picture" />
    </div>
    <h1 class="card-title my-l">@Model.NameLastName</h1>
    <div class="" role="presentation">
        <p class="text-body">@(age.HasValue ? (age+ "y/o"):"")</p>
    </div>
</div>

<ul class="">
    <li class="">
        <div class="carousel-item-left">
            <span class="carousel-item-left">
                <span class="text-left">
                    <span class="card-body text-body"> @Model.Rides.Count() rides published </span>
                </span>
            </span>
        </div>
    </li>
    <li class="">
        <div class="">
            <span class="carousel-item-left">
                <span class="text-left">
                    <span class="card-body text-body"> Member since @Model.RegistrationDate.Value.ToString("MMMM yyyy")</span>
                </span>
            </span>
        </div>
    </li>
</ul>